#include<stdio.h>
#include<math.h>
//prueba switch case
int main(void)
{
    int i,*p ,num, cont, base, exp,bisi=28;
    char opcion;
    i=0;cont=0;
    p=&bisi;

   printf("\n modulo %d", 0%2);
   while(1){
                printf("%d",*p);
            }
   printf("\n cociente %d", 0/2);
   printf("\n bisiesto %d",*p); 
}